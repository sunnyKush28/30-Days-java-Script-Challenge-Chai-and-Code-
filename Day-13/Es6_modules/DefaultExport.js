// Default Export:

// A module can have only one default export.
// Imported using any name.
// Syntax: export default value;

const exportDefault = () => {
    console.log("Value from default export")
}

export default exportDefault